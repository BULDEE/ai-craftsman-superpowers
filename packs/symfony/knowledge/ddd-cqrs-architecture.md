# DDD + CQRS Architecture (moved to core)

> **Deprecated (v3.6.0).** This content was promoted to the language-agnostic core knowledge and will be removed in v4.0.

The framework-agnostic layered structure, use-case pattern, CQRS, and repository pattern now live in:

- `knowledge/ddd/ddd-cqrs-architecture.md`

Symfony-specific implementation details (Doctrine repositories, API Platform, Messenger command bus, Voters) now live in:

- `packs/symfony/knowledge/ddd-symfony-implementation.md`

Update any references to point at those files.
