VERDICT: CHANGES_REQUIRED
REVISION: 84b2350a02918e0c0eab3857ce6c697b77bf4290

FINDINGS:

1. blocking, adapters/copilot/translate.py:88. The shipped PascalCase configuration loses patch identity. GitHub documents apply_patch arriving as tool_name "Edit". Translation preserves "Edit", so a patch without file_path reaches neither gate’s patch reader. Evidence: "core_tool = TOOL_NAMES.get(tool, tool)". In-memory control: camelCase apply_patch identifies /review/.craft-rules.yml; the same arguments under PascalCase Edit produce zero changes. Preserve the mutation type or reject ambiguous Edit arguments. [GitHub contract](https://docs.github.com/en/copilot/reference/hooks-reference#pretooluse--pretooluse)

2. major, adapters/copilot/translate.py:60 and :113. Translation errors become successful empty payloads. Evidence: "except ValueError: return {}" and "payload = {}", followed by "return 0". Malformed toolArgs for create produces Write with empty arguments, which both gates let through. A malformed envelope produces an empty tool name, triggering pre-tool-use.sh:35’s default "exit 0". Reject unreadable envelopes and missing required write arguments.

3. major, adapters/copilot/pre-tool-use.sh:39. The adapter discards a required content correction while permitting the original write. Evidence: "2>&1 >/dev/null". pre-write-check.sh:189 onward handles a sole PHP001 violation by returning updatedInput containing declare(strict_types=1), then exiting 0. The adapter suppresses that JSON and exits 0, allowing the uncorrected PHP file to land. Translate the correction back to Copilot arguments, or deny when it cannot be applied.

4. major, adapters/copilot/post-tool-use.sh:22. A precreated symlink at the predictable temporary path can make the hook truncate another writable file before validation starts. Evidence: ">/tmp/craftsman-copilot-post.$$". The subsequent removal does not undo target corruption. Use an exclusively created temporary file with cleanup on exit.

5. minor, tests/meta/test-fresh-install.sh:58. The fresh-install check retains Python import paths from its caller. With PYTHONPATH pointing to checkout/hooks/lib, an archive missing v4a_patch.py can still import the checkout copy through write_mirror.py:55. Evidence: the only unset statement is "unset CLAUDE_CODE_SESSION_ID CLAUDECODE CRAFTSMAN_HEADLESS_VERIFY"; Python runs without environment isolation. Clear Python path overrides and verify imported project modules originate inside INSTALL.

COMMANDS RUN:

cat, rg, git diff: read the supplied patch, scoped changes, provenance, fixtures and consuming code.
git rev-parse/status: initially confirmed 84b2350 and clean tree; HEAD later advanced to f9f32f5, changing only two documentation files outside this review.
python3 -B in-memory probes: confirmed patch alias loss, empty malformed arguments, missing content and successful malformed-envelope translation.
Python/jq host probes: captured Claude/Codex payloads detect correctly; adding timestamp changes both classifications to copilot.
JSON/shlex inspection: 18 nonempty statusMessage strings; adapter commands tokenize into bash plus one quoted path.
Official documentation reads: statusMessage is documented by [Claude Code](https://code.claude.com/docs/en/hooks) and [Codex](https://developers.openai.com/codex/hooks).
One heredoc probe was blocked by the read-only sandbox; rerun using python3 -c.

NOT VERIFIED:

No suites, hooks, archive builds or live host consumers executed. No files modified or work delegated.

Copilot’s contract leaves file-tool argument schemas, PascalCase PostToolUse matcher aliasing, and whether shell resultType "success" proves process exit zero insufficiently specified. _decode_copilot currently promotes success to succeeded, which the verification consumer accepts as passing evidence; the shipped Copilot adapter does not wire that consumer.

Actual cloud shell execution, timeout behavior, stderr presentation and manifest loading remain unverified. Fresh-install entry points and CLAUDE_PLUGIN_ROOT reference INSTALL, but inherited PATH and Python imports are not fully isolated.