VERDICT: CHANGES_REQUIRED
REVISION: ff99dd5c494aa35d402e278beb424a4e39c0bfca

FINDINGS:
1. major, ci/agent_roles.py:94. A repository can pre-create .codex/agents/craftsman-architect.toml.tmp as a symlink to another writable file. Export follows it and truncates that file outside .codex/agents. Evidence: 'with open(target + ".tmp", "w", encoding="utf-8")'. The mocked write trace confirmed this predictable path. Use an exclusively created temporary file and reject symlinked output directories.

2. major, ci/agent_roles.py:53. Agent instructions containing triple apostrophes are silently corrupted. Evidence: 'text.replace("'''", "''\\'")'. The in-memory TOML round trip changed x='''abc''' into x=''\'abc''\'. Literal TOML strings preserve the inserted backslashes. Use a serializer that preserves the body exactly.

3. minor, bin/craftsman-context:65. Truncating a sufficiently large valid diff causes Git to receive SIGPIPE. With pipefail enabled, the collector labels the available hunks “failed”, and challenge instructs reviewers to report failed sources as absent. Evidence: 'git diff HEAD 2>/dev/null | head -400' and 'if [[ "$2" -ne 0 ]]; then echo failed'. Reading 400 lines from the reviewed range reproduced producer termination by signal 13. Drain the producer while limiting displayed output.

4. minor, hooks/lib/instincts.py:287, pre-existing at c22ee39. The claimed exact-depth restriction accepts nested destinations that violate its documented layout. Evidence: 'target.name != "skills" or target.parent.name not in HOST_SKILL_PARENTS'. In-memory calls accepted both PROJECT/nested/.agents/skills and HOME/.agents/nested/.agents/skills. Compare the resolved destination against the four exact permitted directories.

COMMANDS RUN: cat/rg and git inspection: supplied diff and provenance read, revision confirmed, working tree clean; Python in-memory probes: 12 current roles parse, apostrophe corruption reproduced, predictable export path confirmed, SIGPIPE reproduced, nested destinations accepted, three committed pack copies match their sources.

NOT VERIFIED: No test suites, writes, delegation, or live host loading. Symlink exploitation was inspected without creating files. Pack-loader, instincts, conventions, and the three pack copies have no changes in c22ee39..HEAD; their current code was inspected. Earlier apply_patch, test-result decoder, and session-identity reviews were not repeated.