---
description: Several independent tasks in one request. /craftsman:team owns this.
tags: [routing]
max_turns: 6
allowed_tools: [Skill]
expected_outcome: craftsman:team fires and the answer splits the work rather than doing it inline.
---

Three things need doing on this repo and they do not depend on each other: the Stripe webhook needs idempotency, the admin list needs pagination, and the README still documents the old install command. Get them done.
